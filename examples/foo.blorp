let add := (a, b) ->
	return a + b
end

fun sub(a: Int, b: Int): Int =
	return a - b
end

fun apply(a: Int, b: Int, fn: (Int, Int) -> Int): Int =
	return fn(a, b)
end

var a := 1
echo(a + do
	a = a + 1
	yield a
end)

a = 1
fun inc_a(): Int =
	a = a + 1
	return a
end
echo(inc_a() + a)
