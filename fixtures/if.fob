let x := 7
let simple := if true 10 else 20
let compared := if x > 5 "big" else "small"
let block := if x > 10 do
    yield "large"
end else do
    yield "small"
end

echo("simple: " <> simple)
echo("compared: " <> compared)
echo("block: " <> block)
