import "modules/first.fob" as first
import "modules/second.fob" as second

const START: Int = 0

fun main(): () =
    echo(first::next(START))
    echo(second::next(START))
    echo(first::next(START))
end
